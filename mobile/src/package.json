{
  "name": "ttrpg-mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --port 3001",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "ios": "nuxt generate && npx cap sync ios && npx cap open ios",
    "android": "nuxt generate && npx cap sync android && npx cap open android",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/push-notifications": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@ionic/vue": "^8.0.0",
    "@microsoft/signalr": "^8.0.7",
    "ionicons": "^7.0.0",
    "nuxt": "^3.12.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@nuxtjs/tailwindcss": "^6.0.0",
    "typescript": "^5.4.0"
  }
}
