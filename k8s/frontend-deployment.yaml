apiVersion: apps/v1
kind: Deployment
metadata:
  name: ttrpg-ui
  namespace: ttrpg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ttrpg-ui
  template:
    metadata:
      labels:
        app: ttrpg-ui
    spec:
      containers:
        - name: ttrpg-ui
          image: ttrpg-ui:local
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: NUXT_API_BASE_URL
              value: http://ttrpg-api
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: "3000"
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
