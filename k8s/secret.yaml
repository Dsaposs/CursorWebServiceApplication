# LOCAL DEVELOPMENT ONLY — do not use in production and do not commit with real values.
#
# This file is intentionally NOT included in kustomization.yaml.
# For production, use sealed-secret.yaml (generated by scripts/seal-secrets.ps1).
#
# To apply for local cluster testing (kind / minikube):
#   1. Replace the placeholder values below with real secrets.
#   2. kubectl apply -f k8s/secret.yaml
#   3. Do NOT commit the file after editing — discard your changes when done.

apiVersion: v1
kind: Secret
metadata:
  name: ttrpg-api-secret
  namespace: ttrpg
type: Opaque
stringData:
  Jwt__Key: "ChangeThisKubernetesJwtSigningKeyToARealLongRandomValue123!"
  Seed__AdminPassword: "ChangeThisKubernetesAdminPassword123!"
