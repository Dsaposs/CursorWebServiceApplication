apiVersion: v1
kind: Service
metadata:
  name: notes-api
  namespace: notes
spec:
  type: ClusterIP
  selector:
    app: notes-api
  ports:
    - name: http
      port: 80
      targetPort: http
apiVersion: v1
kind: Service
metadata:
  name: notes-api
  namespace: notes
spec:
  type: ClusterIP
  selector:
    app: notes-api
  ports:
    - name: http
      port: 80
      targetPort: http
